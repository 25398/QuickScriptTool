# Build output
build/
out/
dist/
driver/qst_vhid/bin/
driver/qst_vhid/obj/
driver/qst_vhid/package/
driver/qst_vhid/certs/
driver/qst_vhid/install_log.txt
# _elevate_install.ps1 is shipped with the product (tracked)
*.obj
*.exe
*.pdb
*.ilk
*.tlog
*.lastbuildstate
*.recipe
*.sys
*.cat

# IDE / temp
.vscode/
.tmp/
*.user
*.suo
*.sdf
*.opensdf
*.VC.db
*.VC.opendb

# Third-party binaries (install via scripts/setup_opencv.ps1)
third_party/

# 例外：src/third_party 是**随源码分发的单头依赖**（nlohmann/json.hpp），
# 必须入库 —— 否则新克隆编译不过（src/agent_core.h 直接 include 它）。
# 这条规则是 2026-09-19 首次跑 CI 时暴露的：CI 报
#   src\agent_core.h(16): error C1083: Cannot open include file: 'nlohmann/json.hpp'
# 根因就是 third_party/ 这条规则把它连带忽略了。
# 注意顺序：先重新包含目录本身，再包含其内容，否则 git 不会递归恢复。
!src/third_party/
!src/third_party/**

# Reference / analysis code
flameshot_analysis/
fetched_*.cpp
.cvresearch/

# Research scratch（调研草稿不入公开仓库；office 文档 IO 报告被 AGENTS.md 引用，保留）
.research/*
!.research/office-document-io-report.md
.dsh-research/

# 误提交的 CLI 输出残留（commit 09eb790）
query

# 遗留空目录
archive/

# Garbled / backup doc variants (e.g. *.md.fixed)
*.md.fixed

# WorkBuddy AI project data (local notes / memory; not for the public repo)
.workbuddy-ai/

# Legacy stub
QuickScript/

# Temp source files
*.tmp

# Host-written extension bridge token (unpacked sideload)
extension/edge/bridge_runtime.json

# Website download binaries (generated; upload with site deploy)
website/downloads/*.exe
website/downloads/*.zip
